spring:
  application:
    name: order-service
  jpa:
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    generate-ddl: true
  datasource:
    url: jdbc:postgresql://192.168.228.2:5432/order-db?createDatabaseIfNotExist=true
    username: postgres
    password: order123
    driver-class-name: org.postgresql.Driver
server:
  port: 9996

